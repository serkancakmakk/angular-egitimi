import { RenklendirDirective } from './renklendir.directive';

describe('RenklendirDirective', () => {
  it('should create an instance', () => {
    const directive = new RenklendirDirective();
    expect(directive).toBeTruthy();
  });
});
